"""Optional, framework-specific integrations.

These are thin conveniences on top of the framework-agnostic core. Importing a
submodule here does not pull in the framework unless you actually use a code path
that needs it.
"""
